in frontend
you have already package.json and lock files
now do
npm install express
sudo apt install npm
npm install express (node_modules will be created)
npm install experss body-parser axios
node app.js
now you can access frontend
================================
================================
================================
create .env file and paste the mongodb_url
python3 --version
python3 app.py
pip3 install -r requirements.txt  (pip3 not found)
sudo apt update
sudo apt install python3-venv python3-pip
pip3 --version (it should show)
python3 -m venv venv
source venv/bin/active
pip install -r requriementst.txt
python3 app.py
